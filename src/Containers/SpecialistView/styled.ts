import styled from 'styled-components';

export const SpecialistViewContainer = styled.div`
  background-color: ${props => props.theme.colors.grey_lightest};
  width: 100%;
  position: relative;
  overflow: hidden;
`;