import styled from'styled-components';

export const ClientViewContainer = styled.div`
  background-color: ${props => props.theme.colors.grey_lightest};
  width: 100%;
  position: relative;
  overflow: hidden;
`;

export class ClientViewMain {
}